import { createTheme } from "@mui/material/styles";

export default createTheme({
  palette: {
    background: {
      default: "#FAF8F0",
    },
    primary: {
      main: "#F27D42",
      dark: "#DF622C",
      light: "#F07C41",
      contrastText: "#FFFFFF",
    },
  },
});
